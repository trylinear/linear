<div id="post-{{_id}}" class="post">

    <img src="{{createdBy.avatar}}" width="50" height="50" class="avatar">

    <div class="wrapper">

        <div class="title"><a href="/post/{{slug}}/{{_id}}/">{{title}}</a></div>

        <div class="description">{{{markdown contents}}}</div>

        <div class="info">

            <div class="posted-by">

                {{__ 'Posted by'}}

                {{#if createdBy._id}}
                <a href="/profile/{{createdBy._id}}/" class="name">{{createdBy.name}}</a>
                {{else}}
                <i>{{__ 'deleted'}}</i>
                {{/if}}

                <span class="date">
                    <a href="/post/{{slug}}/{{_id}}/">{{relativeTime createdAt}}</a>
                </span>

                {{#if editable}}
                <span class="admin-links">
                    &mdash;
                    <a href="#edit">Edit</a> <a href="#delete">Delete</a>
                </span>
                {{/if}}

            </div>

        </div>

    </div>

</div>
